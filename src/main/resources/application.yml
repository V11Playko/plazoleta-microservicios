server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usuarios
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Power Up"
appVersion: "1.0.0"
